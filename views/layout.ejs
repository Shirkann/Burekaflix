<!DOCTYPE html>
<html lang="he" dir="rtl">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= typeof title !== 'undefined' ? title : 'BurekaFlix' %></title>

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Heebo:wght@300;400;500;700;800&display=swap"
      rel="stylesheet"
    />

    <link rel="stylesheet" href="/css/main.css" />
  </head>
  <body>
    <div class="container">
      <header class="header">
        <div class="logo">BurekaFlix</div>

        <nav class="nav">
          <% if (currentUser) { %>
          <!-- 'בית' מפנה ל-root, שהשרת שלך כבר מפנה ממנו ל-/catalog -->
          <a href="/">דף הבית</a>

          <!-- פרופילים: הנתיב קיים אצלך (app.use('/profiles', profiles)) -->
          <a href="/profiles">פרופילים</a>

          <!-- התנתקות: ברוב המקרים זה POST. אם אצלך זה GET – ראה ההערה מתחת. -->
          <form action="/logout" method="post" style="display: inline">
            <button
              class="btn"
              type="submit"
              style="padding: 8px 12px; font-size: 14px"
            >
              התנתקות
            </button>
          </form>
          <% } else { %>
          <a href="/login">כניסה</a>
          <a href="/register">הרשמה</a>
          <% } %>
        </nav>
      </header>

      <%- body %>
    </div>

    <script src="/js/main.js" defer></script>
  </body>
</html>
